{ 
  "name": "ai-agent",
  "description": "for the AI Agent project, retain this package.json file only sets up Husky for managing Git hooks.",
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7"
  }
}

